#the quick brown fox
#
# The above comment is needed exactly as is
# for this test to work - do not change it.

main()
{
    file := open("io.rr", "r");
    s := read(file, 1);
    a := read(file, 3);
    b := read(file, 1);
    c := read(file, 5);
    if((a == "the") && (c == "quick"))
    {
        ret 0;
    }
    else
    {
        ret 1;
    }
}
