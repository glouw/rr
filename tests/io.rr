#the quick brown fox

main()
{
    file := open("io.rr", "r");
    s := read(file, 1);
    a := read(file, 3);
    b := read(file, 1);
    c := read(file, 5);
    if((a == "the") && (c == "quick"))
    {
        ret 0;
    }
    else
    {
        ret 1;
    }
}
