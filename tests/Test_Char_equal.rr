C(str)
{
    str += str[0];
    str[0] = "T";
    Assert(str[0] == "T");
    Assert("T" == str[0]);
    Assert(str[0] == str[0]);
}

B(str)
{
    ret C(str);
}

A(str)
{
    ret B(str);
}

Main()
{
    str := "test" + "test";
    A(str);
    ret 0;
}
