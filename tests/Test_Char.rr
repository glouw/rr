Fun()
{
    a := "asdf";
    ret a[0];
}

Main()
{
    Fun(); # Should cleanup.
    Assert(Type(Fun()) == "string"); # PROMOTION.
    x := Fun();
    Assert(Type(x) == "string");
    str := "string";
    str[0] = "S";
    str[1] = "T";
    Print(str);
    Assert(str == "STring");
    ret 0;
}
