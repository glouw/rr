Main()
{
    a := 1;
    b := a += 1;
    c := 1;
    d := c *= 1;
    Assert(!(a ? b));
    Assert(!(c ? d));
    Assert(a == 2);
    Assert(b == 2);
    Assert(c == 1);
    Assert(d == 1);
    ret 0;
}
