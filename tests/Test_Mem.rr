Main()
{
    string := "string";
    a := {
        "key": string
    };
    b := a["key"];
    Assert(!(a["key"] ? b));
    value := 0;
    queue := [value];
    Assert(!(queue[0] ? value));
    ret 0;
}
