Main()
{
    Assert(Type("asdf") == "string");
    Assert(Type([]) == "queue");
    Assert(Type({}) == "map");
    Assert(Type(false) == "bool");
    Assert(Type(true) == "bool");
    Assert(Type(1) == "number");
    Assert(Type(null) == "null");
    Assert(Type(Main) == "function");
    Assert(Type(Open("Test_Type.rr", "rw")) == "file");
    Assert(Type("a"[0]) == "char");
    ret 0;
}
