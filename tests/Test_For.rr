Main()
{
    a := [ 0, 1, 2, 3 ];
    sum := 0;
    for(i := 0; i < Len(a); i += 1)
    {
        sum += a[i];
    }

    j := 44; # Stack gaurd to ensure nothing was clobbered.

    Assert(j == 44);
    Assert(sum == 6);
    ret 0;
}
