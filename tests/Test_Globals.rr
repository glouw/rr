A := 123;
B := A + 1;

Add(a, b)
{
    ret a + b;
}

C := Add(1, Add(A, B));
D := {
    "value" : 100
};

E := Add(D["value"], C);

Main()
{
    Assert(A == 123);
    Assert(B == 124);
    Assert(C == 248);
    Assert(E == 348);
    ret 0;
}
