Main()
{
    a := [0,1,2,3,4];
    b := { "key": "value"};
    c := "string";
    Del(a, 0);
    Del(a, 1);
    Del(a, -1);
    Del(a, Len(a) - 1);
    Del(b, "keyadfsafd");
    Del(c, 0);
    Del(c, -1);
    Assert(a == [1]);
    Assert(b == {"key" : "value"});
    Assert(c == "trin");
    ret 0;
}
