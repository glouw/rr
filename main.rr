fun(a)
{
}

main()
{
    list := [ 0, 1, 2, 3];
    fun(&list);
    index := 0;
    while(index < len(list))
    {
        index += 1;
    }
    assert(index == 4);
    del(list["key"]);
    keys(list["key"]);
}
