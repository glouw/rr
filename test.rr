inc examples.leetcode.lib.PQ;

Compare(a, b)
{
    ret *a < *b;
}

Main()
{
    pq := PQ(Compare);
    PQ_Push(pq, 9);
    PQ_Push(pq, 0);
    PQ_Push(pq, 1);
    PQ_Push(pq, 2);
    f := *PQ_Front(pq);
    PQ_Pop(pq);
    PQ_Pop(pq);
    Print(f);
    ret 0;
}
